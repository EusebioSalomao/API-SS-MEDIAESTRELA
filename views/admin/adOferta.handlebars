<div class="container">
    <div class="row">
        <div class="col-6 offset-3">
{{#each erros}}
<div class="alert alert-danger text-center">{{texto}}</div>
{{else}}
{{/each}}
            <h2>Nova oferta:</h2>
<hr>
            <div class="card">
                <div class="card-body">
                    <form action="/ofertas/add" method="post">
                        <p class="lead bg-secondary px-3 p-1 text-light">{{membro.nome}}</p>
                        <label for="data">Data:
                            <input type="date" name="data" class="form-control">
                        </label>
                        <label for="valor">Valor: <input type="number" name="valor" class="form-control"></label>
                        <label for="tesoResp">Tesoureiro: <input type="text" name="tesoResp" class="form-control"
                                disabled> </label>

                        <input type="hidden" name="id" value="{{membro._id}}">
                        <button class="btn btn-primary mb-2">Adicionar oferta</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
<hr>
<div class=" text-center lead">Pagina oficial da Sociedade Média de Nova Estrela - > Adicionar contribuição de quotas de {{membro.nome}}</div>